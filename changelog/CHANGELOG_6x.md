# Changelog

## [6.7.3] - 2026-09-06
### fix
- Trivia solo-mode isolation patch: preserved the selected solo or cooperative mode in every session, restricted solo answers to the member who launched the game, kept cooperative answers open to other members, displayed the active mode directly on the question card, clarified the slash-command option, added a permanent access regression, and bumped Lumi to 6.7.3 without a Prisma schema change.

## [6.7.2] - 2026-09-02
### fix
- Temporary-voice creation stability patch: protected channels while their database row, member move and control panel are being created; delayed cleanup attempts that arrive during this protected window; serialized panel creation per channel to prevent duplicate concurrent sends; replaced the fragile panel record update with a non-throwing guarded update that removes an orphan panel if the row genuinely disappeared; excluded pending channels from daily reconciliation; added permanent race-protection regressions; and bumped Lumi to 6.7.2 without changing the Prisma schema or slash commands.

## [6.7.1] - 2026-09-01
### fix
- Free-game duplicate-posting and dependency-security patch: split Steam and Epic publication state per guild, changed automatic posts to include only newly discovered offers instead of every remaining active game, added a bounded per-offer history so one expired promotion cannot republish another store's catalog, migrated the previous combined signature without a first-run repost, kept manual posts synchronized with both source states, added expiry and legacy-state regressions, overrode Prisma CLI's vulnerable `mysql2` 3.15.3 dependency with 3.24.2 to restore a zero-vulnerability npm audit without downgrading Prisma, and bumped Lumi to 6.7.1 without a Prisma schema change.

## [6.7.0] - 2026-08-31
### add
- Local assistant tools and stable-maintenance release: added private `/calculator` sessions with a compact button keypad, protected ownership, one active session per member, ten-minute expiry, backspace, reset, close, decimal and signed values, guarded division by zero and non-finite results, clean shutdown disposal and no Neon storage; added `/convert duration` for seconds, minutes, hours and days plus `/convert timezone` across twenty curated IANA zones with optional dates, seasonal clock-change handling, invalid date/time protection and no external API or stored input; connected both tools to local `/lumi ask` discovery and added permanent arithmetic, component, duration, date, timezone and daylight-saving regressions; bounded Trivia redirects, updated Axios to 1.20.0, retained stable Prisma 7.10 while Prisma 8 remains a release candidate, completed the final dependency, secret, permission, network, timer, database and shutdown audit with zero npm vulnerabilities and verified package signatures, refreshed private and public maintenance documentation, and bumped Lumi to 6.7.0.
g

## [6.6.0] - 2026-08-30
### improve
- Long-term stability and supervision update: completed a full audit of the 121 JavaScript files, 32 slash commands, Discord permission gates, bounded in-memory sessions, Neon cleanup paths, external API calls and secret-safe errors; kept Prisma ORM pinned to the stable 7.10.0 release instead of adopting the Prisma 8 release candidate; removed inactive fishing-card deletion so lifetime records and total catches remain intact until the member or Lumi leaves the guild; expanded public runtime health with Discord disconnect, reconnect and resume counters, Neon last-success and failure history, process start time and daily-maintenance state; added a responsive maintenance card to `/status`; made daily maintenance reconcile temporary voice channels and retry thirty minutes after a Neon outage; bounded graceful shutdowns to fifteen seconds so Render can always replace a stuck process; added response-size and redirect limits to weather, Steam and Epic requests; strengthened release checks for fishing retention, runtime counters and status-page JavaScript; refreshed private architecture, maintenance and project notes plus public privacy/data documentation; confirmed zero npm audit findings and bumped Lumi to 6.6.0.

## [6.5.0] - 2026-08-29
### add
- Lightweight assistant, games and temporary-voice polish update: added `/reminder` with create, private list and owner-scoped cancel actions, a five-reminder limit per member and guild, a thirty-day maximum, one global restart-safe scheduler, original-channel delivery with DM fallback, database-outage retry behavior and automatic deletion before delivery, cancellation, member departure or guild cleanup; added a five-question French Trivia mode to `/game` using the verified QuizzAPI.fr source, a twenty-question in-memory cache, one answer per player, bounded timers and in-memory scores without sending Discord identity data or creating Neon rows; removed difficulty selection from LUMOT and Memory, randomized LUMOT across all available word lengths and standardized Memory on an eight-pair board; made temporary voice channels remain excluded from generic channel and message logs after any rename by tracking their real IDs, preserved their database row when Discord deletion fails, and clarified the live access status in their panel; retired the public support-server references, kept admin-only `/feedback` available during private-access setup, refreshed public data/privacy/user documentation and local assistant discovery, added focused regression checks, and bumped Lumi to 6.5.0.

## [6.4.0] - 2026-08-28
### add
- Temporary voice control and local presence update: added an automatically maintained control panel inside each temporary voice channel with rename, user limit, open, locked and private modes, individual allow/remove access, ownership transfer and refresh actions; kept `/voice` slash actions as fallbacks and added `/voice private` plus `/voice panel`; replaced restart-fragile in-memory permission restoration with temporary persisted snapshots, access modes and member access state that are reconciled after startup and deleted with the channel; prevented concurrent duplicate voice creation for the same member; added direct Lumi mention replies for greetings, thanks, personality and local command discovery with private-server access checks, a bounded cooldown, no external AI and no stored conversation content; added a `/configuration` toggle for mention replies, persistent-component and assistant regressions, updated public privacy/data documentation and user guidance, and bumped Lumi to 6.4.0.

## [6.3.0] - 2026-08-27
### add
- Local assistant and lightweight community games update: added `/lumi ask` with Fuse.js-powered local command discovery, permission hints, honest fallbacks and developer-command filtering without external AI or stored questions; added `/game` with LUMOT solo/co-op word sessions, accent normalization, repeated-letter scoring, a filtered local French dictionary, recent-answer protection and six-attempt expiry, plus three-size solo Memory grids with protected clicks and in-memory rematches; added staff-only `/poll` using Discord-native polls for two to ten answers, bounded durations and optional multiselect without Neon storage; centralized RPS, TicTacToe, LUMOT and Memory engines under the games feature, moved Level into the utility category, added feature-level regression checks, pinned Node.js 24.20.0 for Render consistency, archived the complete 0.x through 5.x public changelogs without deleting history, refreshed public/private documentation, and bumped Lumi to 6.3.0.

## [6.2.0] - 2026-08-26
### improve
- Command ergonomics and resource-efficiency update: rebuilt `/configuration`, `/moderation` and `/staff` around compact section buttons and native searchable Discord selectors; grouped configuration choices by General, Logs and AutoMod views; exposed current role, member and channel selections directly in focused pickers; supported text and announcement channels where appropriate; removed the unnecessary welcome and goodbye test actions; allowed moderation targets to be selected without relaunching the slash command and reused cached Discord users before making an API request; replaced the long `/roles panel` role-option list with a ten-minute in-memory builder supporting up to ten roles, preview, cancel and publish actions while preserving role hierarchy safeguards and adding no Neon storage; added component-limit and duplicate-ID regressions for the main interactive panels; compacted the public roadmap into durable milestones and future directions; refreshed private architecture, maintenance and project documentation plus the public user guide; and bumped Lumi to 6.2.0.

## [6.1.0] - 2026-08-25
### improve
- Code organization, command UX and runtime maintenance update: removed the unused `/spark` command; simplified `/community` around official status, support, repository, access and policy links; centralized official URLs and replaced the Administrator invite with a scoped permission set; kept lightweight slash entry files while moving the full `/configuration`, `/moderation` and `/staff` panel implementations into dedicated feature modules; streamlined the interaction router with shared imports; allowed Lumi to finish starting when daily Neon maintenance is temporarily unavailable; removed redundant fishing cleanup queries from user commands in favor of the existing daily retention task; added bounded cleanup for temporary log suppressions, channel markers, duel timers, staff sessions and ticket deletion timers; strengthened event-module validation and graceful runtime timer cleanup; updated Prisma packages to 7.10.0; removed empty accidental project directories; refreshed architecture, maintenance, user guide and public README documentation; and bumped Lumi to 6.1.0.

## [6.0.1] - 2026-08-25
### fix
- Steam rate-limit and status-page patch: replaced concurrent temporary-promotion validation bursts with a paced sequential queue and bounded `429` retries, reduced the number of candidates checked per cycle, collapsed validation failures into one safe source warning, prevented automatic free-game posts from publishing or saving incomplete source results during a temporary Steam limit, added a `/ready` fallback and clearer temporary-read messaging to the public status page, and made `docs/vitrine` the single source for public documentation instead of maintaining duplicate copies.

## [6.0.0] - 2026-08-25
### improve
- Reliability, data clarity and public status major update: migrated Lumi to Prisma ORM 7 with the official PostgreSQL driver adapter, an explicit CLI configuration file, a small bounded Neon connection pool and a verified Node/Render-compatible CommonJS runtime; updated the PostgreSQL driver, safely overrode Prisma CLI's vulnerable merge dependency and reduced the full npm audit to zero findings; added a migration that removes the three unused GuildSettings database views while preserving Prisma's required migration history; added daily cleanup for deleted Discord channel and role references, automatic disabling of dependent modules when their required target disappears, and configurable removal of XP profiles inactive for 180 days; repaired Steam free-to-keep discovery by combining featured offers with Steam's 100%-discount search and official app validation, including Dokimon-style promotions, images, prices and parsed offer end dates; simplified `/fish catch` into a compact capture card with rarity, weight, score, five-tier location rating and one weekly rank line; added a responsive public `/status` page for Render, Discord Gateway and Neon while preserving cached `/health` and Discord-focused `/ready` probes; replaced overstated product claims with the honest private Discord assistant identity, simplified the presence to `Version 6.0.0`, created a synchronized `docs/vitrine` delivery folder for public repository files, refreshed maintenance, privacy, architecture, project, protection and README documentation, and bumped Lumi to 6.0.0.

